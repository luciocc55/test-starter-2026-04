generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model StarterPlaceholder {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
